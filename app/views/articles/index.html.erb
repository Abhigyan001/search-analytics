<section class='headline-section'>
  <div class="text-center">
    <button type="button" class="btn btn-info btn-lg analytics-button">
      <%= link_to 'See Search Analytics', analytics_path, {:style=>'color:#FFFFFF;'} %>
    </button>
  </div>
  <%= render partial: '/shared/homepage', locals: { articles: @articles } %>
</section>
